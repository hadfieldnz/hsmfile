from setuptools import setup

setup(
   name='afile',
   version='0.0',
   description='Expedite access to files on a slow network volume',
   author='Mark Hadfield',
   author_email='mark.hadfield@niwa.co.nz',
   url='https://github.com/hadfieldnz/afile',
   packages=['afile'],
   package_dir={'afile':'.'}
)
